<% if File.directory?(@real_path) %>
	<% for entry in Dir.entries(@real_path)[2..] %>
		<%= link_to entry, @relative_path+'/'+entry %>
	<% end %>
<% else %>
	<%= render inline: Kramdown::Document.new(File.read(@real_path)).to_html %>
<% end %>
